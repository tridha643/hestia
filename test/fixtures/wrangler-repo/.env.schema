# minimal varlock schema so the auto-detected `varlock run --` wrapper path is
# exercised end-to-end (composition must not break the boot pipeline)
#
# @defaultRequired=false @defaultSensitive=false
# ----------

FIXTURE_FLAG="on"
